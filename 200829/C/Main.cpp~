#include <iostream>
#include <string>
#include <vector>
#include <math.h>
using namespace std;
int main() {

  long long N;
  cin >> N;
  vector<long long> A(N + 1);
  long long total = 0;
  long long mod = #include <iostream>
#include <string>
#include <vector>
#include <math.h>
    using namespace std;
  int main() {

    long long N;
    cin >> N;
    vector<long long> A(N + 1);
    long long total = 0;
    for (int i = 1; i < N + 1; i++) {
      cin >> A[i];
      total += A[i];
    }
    vector<long long> S(N + 1);
    S[0] = total;
    for (int i = 1; i < N + 1; i++) {
      S[i] = S[i - 1] - A[i];
      //cout << S[i] << endl;
    }

    long long sum = 0;
    for (int i = 1; i < N + 1; i++) {
      sum += (A[i] * S[i]);

    }

    cout << sum % (long long)(pow(10, 9) + 7) << endl;

    return 0;
  for (int i = 1; i < N + 1; i++) {
    cin >> A[i];
    total += A[i]%;
  }
  vector<long long> S(N + 1);
  S[0] = total;
  for (int i = 1; i < N + 1; i++) {
    S[i] = S[i - 1] - A[i];
    //cout << S[i] << endl;
  }

  long long sum = 0;
  for (int i = 1; i < N + 1; i++) {
    sum += (A[i] * S[i]);
    
  }

  cout << sum % (long long)(pow(10, 9) + 7) << endl;
  
  return 0;

}
